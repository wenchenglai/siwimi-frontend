import { substring } from 'siwimi-frontend/helpers/substring';
import { module, test } from 'qunit';

module('Unit | Helper | substring');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = substring([42]);
  assert.ok(result);
});
