import { html2string } from 'siwimi-frontend/helpers/html2string';
import { module, test } from 'qunit';

module('Unit | Helper | html2string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = html2string([42]);
  assert.ok(result);
});
