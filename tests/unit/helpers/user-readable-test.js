import { userReadable } from 'siwimi-frontend/helpers/user-readable';
import { module, test } from 'qunit';

module('Unit | Helper | user readable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userReadable([42]);
  assert.ok(result);
});
